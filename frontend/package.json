{
  "name": "map-travel-frontend",
  "packageManager": "pnpm@10.33.0",
  "private": true,
  "scripts": {
    "vendor:basemap": "node ../scripts/vendor-basemap-assets.mjs",
    "build": "pnpm run vendor:basemap && vite build",
    "dev": "pnpm run vendor:basemap && vite build --watch",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "type": "module",
  "dependencies": {
    "maplibre-gl": "^5.23.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.5"
  }
}
