[package]
name = "migration"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
sea-orm-migration = { version = "2.0.0", default-features = false, features = ["runtime-tokio-rustls", "sqlx-postgres"] }
